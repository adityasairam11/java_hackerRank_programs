package thoughtworks;

public class marsAir {

    /*
         to be placed in global prop ->   String userID="adityasairam11@gmail.com";
    WebDriver driver = new ChromDriver();
    System.setProperty("driver.chrome.driver",pathToDriverFile);
    driver.manage.windows.maximize();
    to be referred -> driver.manage.wait.page.loads();

    driver.get("https://marsair.recruiting.thoughtworks.net/candidate-"+userID);

    String title = driver.getTitiel();

    Assert.assertEquals(title,expectedTitle)

 ExplicitWait wait = new ExplicitWait(10);

    WebElement departDropdown = driver.findElement(By.id("departing");
    WebElement arriveDropdown = driver.findElement(By.id("returning");
    WebElement promoCode = driver.findElement(By.id("promotional_code");
    WebElement searchBtn = driver.findElement(By.cssSelector("#content > form > dl:nth-child(5) > dd > input[type=submit]");
    WebElement successTextBtn = driver.findElement(By.cssSelector("#content > p:nth-child(2)");
    Select select = new Select();



wait.until.expectedConditions.(ElementtobeVisible(departDropDown);
repeat above step for all elements

String methodA(int a, int b){
select = new Select(departDropDown);
select.selectByIndex(a);
select = new Select(arriveDropDown);
select.selectBy.Inndex(b);
promoCode.sendKeys(promoCode);
searchBtn.click();
String sycces = successTextBtn.getText();
return syccess;
}

@test
testPass(){

Assert.assertEquals(methodA(0,5),"Seats available!");
}

@test
failScenario(){
Assert.assertEquals(methodA(0,1),"Unfortunately, this schedule is not possible. Please try again.");
}


     */
}
